<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- product.xml -->

<mapper namespace="product">

    <select id="list" resultType="java.util.Map">
        SELECT cont_no, cont_name, cont_place, cont_img, cont_size
        FROM cont
        ORDER BY cont_no DESC
    </select>

    <select id="mid" resultType="java.util.Map">
        SELECT cate.cate_middle
        FROM cont
                 JOIN cate ON cont.cate_no = cate.cate_no
    </select>

    <select id="suf" resultType="java.util.Map" parameterType="String">
    SELECT cont.cate_no, cont.cont_no, cont.cont_name, cont.cont_place, cate.cate_large, cate.cate_middle
        FROM cont
        JOIN cate ON cont.cate_no = cate.cate_no
     WHERE cate.cate_large = #{cate_large}
    </select>


</mapper>