<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
    <insert id="insert" parameterType="java.util.Map">
        insert into user (user_id,user_pw,user_name,user_email,user_phone,user_birth,user_gender)
        values (#{user_id},#{user_pw},#{user_name},#{user_email},#{user_phone},#{user_birth},#{user_gender})
    </insert>

    <!-- 로그인 -->
    <select id="Login" parameterType="com.habit.member.MemberDTO" resultType="com.habit.member.MemberDTO">
        Select user_id, user_pw
        from user
        where user_id=#{user_id} and user_pw=#{user_pw}
    </select>

    <!-- 아이디 찾기 -->
    <select id="findId" parameterType="com.habit.member.MemberDTO" resultType="com.habit.member.MemberDTO">
        SELECT user_id
        from user
        where user_email=#{user_email}
    </select>

    <!--비밀번호 찾기 -->
    <select id="findPw">
        SELECT
    </select>

</mapper>